apiVersion: v1
kind: Service
metadata:
  name: ruby-svc
  labels:
    run: ruby-svc
spec:
  selector:
    run: ruby-deployment
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
